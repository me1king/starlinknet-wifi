{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy && node scripts/whatsapp-bridge.cjs & next start -p ${PORT:-3000}",
    "restartPolicyType": "ON_FAILURE",
    "numReplicas": 1
  }
}
